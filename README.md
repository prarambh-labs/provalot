# provalot

Deterministic evidence gate for coding agents. A hook for Claude Code and Codex CLI that keeps a local ledger of what actually ran and changed, and refuses "done" when the final message claims something the ledger cannot back.

