[package]
name = "provalot"
version = "0.1.0"
edition = "2021"
description = "Deterministic evidence gate for coding agents"
license = "MIT"
repository = "https://github.com/provalot/provalot"

[[bin]]
name = "provalot"
path = "src/main.rs"

[dependencies]
clap = { version = "4", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
regex = "1"

[dev-dependencies]
tempfile = "3"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true
